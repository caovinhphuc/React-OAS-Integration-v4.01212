/**
 * 📊 Custom Metrics Widget Template
 *
 * Create your business-specific dashboard widgets here.
 * Replace example data with your actual API calls.
 *
 * NOTE: This component is currently disabled as it uses @mui/material
 * which is not in the project dependencies. To use this component,
 * either install @mui/material or convert it to use Ant Design.
 */

import React from 'react'
// import { Card, Typography, Spin, Alert } from "antd";
// import {
//   LineChart,
//   Line,
//   XAxis,
//   YAxis,
//   CartesianGrid,
//   Tooltip,
//   Legend,
//   ResponsiveContainer,
// } from "recharts";

// Temporarily disabled - requires @mui/material or conversion to Ant Design

const YourMetricsWidget = () => {
  // Component temporarily disabled - requires @mui/material or conversion to Ant Design
  // To enable: Install @mui/material or convert to use Ant Design components

  return (
    <div style={{ padding: 16, textAlign: 'center' }}>
      <p>⚠️ This component is currently disabled.</p>
      <p>It requires @mui/material which is not in the project dependencies.</p>
      <p>To use this component, either install @mui/material or convert it to use Ant Design.</p>
    </div>
  )
}

export default YourMetricsWidget
